// Construa um algoritmo que calcule a área de um triângulo: A = (b x h)/2

programa{
  funcao inicio() {

    real b=3.2, h=5.6, area

    area = (b * h) / 2
    
    escreva("A área é ", area)
  }
}